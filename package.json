{
  "name": "delima-fullstack",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix delima-project-website ci && npm --prefix delima-project-website run build",
    "build": "npm run vercel-build"
  }
}
