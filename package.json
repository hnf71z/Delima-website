{
  "name": "delima-fullstack",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix delima-project-website ci && npm --prefix delima-project-website run build && rm -rf .next && cp -R delima-project-website/.next .next",
    "build": "npm run vercel-build"
  }
}
