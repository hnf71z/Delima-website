{
  "name": "delima-fullstack",
  "private": true,
  "workspaces": [
    "delima-project-website"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "npm --prefix delima-project-website run dev",
    "build": "npm --prefix delima-project-website run build",
    "start": "npm --prefix delima-project-website run start",
    "vercel-build": "npm --prefix delima-project-website run vercel-build"
  }
}
